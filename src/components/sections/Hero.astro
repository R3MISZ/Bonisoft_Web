---
/**
 * Opening section. Purely static: no client directive, no JavaScript shipped.
 * Headline text stays in the component because it appears exactly once.
 *
 * Both product shots are cutouts with transparency, so the phone can sit in
 * front of the portal without a frame. Width and height are set on the tags so
 * the browser reserves the space before the files arrive.
 */
const portal = { src: "/product/portal-dashboard.webp", w: 1919, h: 1164 };
const mobile = { src: "/product/mobile-iphone.webp", w: 1600, h: 1164 };
---

<section id="top" class="bg-ink-900 text-white">
  <div
    class="mx-auto grid max-w-6xl gap-14 px-6 pt-36 pb-28 lg:grid-cols-[1fr_1.05fr] lg:items-center lg:gap-10"
  >
    <div>
      <h1
        class="text-4xl leading-tight font-semibold tracking-tight sm:text-5xl"
      >
        Die App, die Ihre Leute{" "}
        <span class="text-brand-500">freiwillig öffnen.</span>
      </h1>

      <p class="mt-6 max-w-xl text-lg text-ink-300">
        Mitarbeitende sammeln Punkte für erledigte Aufgaben und lösen sie gegen
        Tankkarte, Gutscheine oder Auszahlung ein. Weil sie deshalb ohnehin
        hineinschauen, kommen dort auch Schichtplan, Nachweise und Kennzahlen an.
      </p>
    </div>

    <div class="relative">
      <img
        src={portal.src}
        width={portal.w}
        height={portal.h}
        alt="Das Bonisoft-Portal mit dem Dashboard einer Führungskraft"
        fetchpriority="high"
        decoding="async"
        class="stage stage-portal w-full"
      />

      <img
        src={mobile.src}
        width={mobile.w}
        height={mobile.h}
        alt="Die Bonisoft-App auf dem Smartphone"
        decoding="async"
        class="stage stage-mobile absolute bottom-0 left-40 w-[52%] translate-y-[12%]"
      />
    </div>
  </div>
</section>

<style>
  /*
   * Entrance on load: the portal settles in first, the phones follow.
   * `translate-y-[12%]` on the phone uses the `translate` property, so the
   * keyframes below are free to use `transform` without fighting it.
   */
  .stage {
    opacity: 0;
    animation: stage-in 0.7s cubic-bezier(0.22, 1, 0.36, 1) forwards;
  }

  .stage-portal {
    animation-delay: 0.1s;
  }

  .stage-mobile {
    animation-delay: 0.55s;
  }

  @keyframes stage-in {
    from {
      opacity: 0;
      transform: translateY(1.5rem) scale(0.97);
    }
    to {
      opacity: 1;
      transform: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .stage {
      animation: none;
      opacity: 1;
    }
  }
</style>
